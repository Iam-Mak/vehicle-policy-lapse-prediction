services:

  api-gateway:
    build: ./api-gateway
    container_name: api-gateway
    ports:
      - "8000:5000"
    depends_on:
      - model-service

  model-service:
    build: ./model_service
    container_name: model-service
    ports:
      - "8001:8001"